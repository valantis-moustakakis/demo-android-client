<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Thesis" parent="Theme.Material3.Dark">
        <item name="android:windowBackground">@color/customDark</item>
        <item name="colorPrimary">@color/customLight</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>